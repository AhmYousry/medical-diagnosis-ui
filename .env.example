# Base URL of the backend API (no trailing slash)
# In production with docker-compose.prod.yml this is handled via the nginx proxy,
# so VITE_API_URL should be your public domain (e.g. https://yourdomain.com).
# In local dev, leave it empty — the Vite proxy handles /api requests.
VITE_API_URL=
